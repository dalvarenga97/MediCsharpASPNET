@model IEnumerable<MediCsharpASPNET.Models.Consulta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaConsulta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Diagnostico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.NombreDoctor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.NombrePaciente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaConsulta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Diagnostico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.NombreDoctor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.NombrePaciente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Consulta }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Consulta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Consulta })
        </td>
    </tr>
}

</table>
